<% if can? :create, Comment %>
  <div id="create-comment">
    <%= form_for [commentable, Comment.new] do |f| %>
      <p>
        <strong>Crea tu comentario</strong> <br />
        <%= f.text_area :comment, :size => "70x5" %>
      </p>
    
      <p><%= f.submit %></p>
    <% end %>
  </div>
<% else %>
  <p>Debes iniciar sesión para poder agregar un comentario.</p>
<% end %>